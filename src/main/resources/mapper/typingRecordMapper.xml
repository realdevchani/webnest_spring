<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.webnest.mapper.TypingRecordMapper">
    <!-- SEQ로 PK 생성 -->
    <insert id="insert" parameterType="TypingRecordVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT SEQ_TYPING_RECORD.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO TBL_TYPING_RECORD
        (ID, TYPING_RECORD_TYPIST, TYPING_RECORD_ACCURACY, TYPING_RECORD_TIME,
        USER_ID, TYPING_CONTENTS_ID)
        VALUES
        (#{id}, #{typingRecordTypist}, #{typingRecordAccuracy}, #{typingRecordTime},
        #{userId}, #{typingContentsId})
    </insert>

    <!-- 특정 유저의 타자 기록 조회 -->
    <select id="findByUserId" resultType="TypingRecordDTO">
        SELECT *
        FROM TBL_TYPING_RECORD
        WHERE USER_ID = #{userId}
        ORDER BY ID DESC
    </select>

</mapper>